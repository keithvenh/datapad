<section>

    <div class="section-header">
        <p>BOOKS</p>
    </div>

    <ul>
    
        <% @books.each do |book| %>
    
            <li class="admin-primary"><%= link_to(book.title, edit_book_path(book)) %></li>

        <% end %>

    </ul>

</section>